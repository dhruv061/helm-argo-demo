{{- if .Values.applications.backend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.applications.backend.service.name }}
  namespace: {{ .Values.applications.namespace }}
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: {{ .Values.applications.backend.service.port }}
      targetPort: {{ .Values.applications.backend.service.targetPort }}
  selector:
    app: backend
{{- end }}
